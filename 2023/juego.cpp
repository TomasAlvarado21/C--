#include <bits/stdc++.h>

using namespace std;
int techo(){
    
}

int main(){

    long long t;
    cin >> t;
    while(t--){
        long long hc, dc, hm, dc, k, w, a;
        cin >> // variables

        bool ok = false;

        for (int i=0, i<=k, i++){
            long long turnplayer = techo(hm, dc+(k-i)*w);
            long long turnmonster = techo(hc + k*a, dm);

        }
            if (turnplayer > turnmonster) return 1;
            else return 0;
    }

}