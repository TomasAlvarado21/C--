#include <bits/stdc++.h>
#include <iostream>
#include <string>
using namespace std;

// Function to find the maximum pair of numbers in an array that
// the maximum common divisor of the two numbers > 1
void findMaxPair(int arr[], int n){
    // we need to reorder the array in order to find the maximum
    // common divisor
    int temp;
}

// Function to find the maximum common divisor of a number and the double of the second number
int findMaxCommonDivisor(int a, int b){
    // we need to find the maximum common divisor of a and 2*b
    int temp;
    
    if (a == 0 || b == 0)
        return 0;

}


